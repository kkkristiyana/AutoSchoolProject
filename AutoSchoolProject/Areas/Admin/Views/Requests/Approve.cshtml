@model ApproveEnrollmentRequestViewModel

@{
    ViewData["Title"] = "Одобри заявка";
}

<h2 class="mb-3">✅ Одобри заявка #@Model.RequestId</h2>

<div class="card mb-3">
    <div class="card-body">
        <p class="mb-1"><strong>Име:</strong> @Model.FullName</p>
        <p class="mb-1"><strong>Телефон:</strong> @Model.PhoneNumber</p>
        <p class="mb-1"><strong>Курс:</strong> @Model.CourseName</p>
        <p class="mb-0"><strong>Старт:</strong> @Model.PreferredStartDate.ToString("dd.MM.yyyy")</p>
    </div>
</div>

<div asp-validation-summary="All" class="text-danger"></div>

<div class="card">
    <div class="card-body">
        <form asp-action="Approve" method="post">
            @Html.AntiForgeryToken()
            <input type="hidden" asp-for="RequestId" />
            <input type="hidden" asp-for="FullName" />
            <input type="hidden" asp-for="PhoneNumber" />
            <input type="hidden" asp-for="CourseName" />
            <input type="hidden" asp-for="PreferredStartDate" />

            <div class="mb-3">
                <label asp-for="UserEmail" class="form-label">Имейл на потребителя</label>
                <input asp-for="UserEmail" class="form-control" readonly />
            </div>

            <div class="mb-3">
                <label asp-for="SelectedRole" class="form-label">Роля при одобряване</label>
                <select asp-for="SelectedRole" asp-items="Model.AvailableRoles" class="form-select"></select>
                <span asp-validation-for="SelectedRole" class="text-danger"></span>
            </div>

            <div class="mb-3">
                <label asp-for="AdminNote" class="form-label">Бележка към потребителя</label>
                <textarea asp-for="AdminNote" class="form-control" rows="3"></textarea>
                <div class="form-text">Бележката ще се показва в профила на курсиста след одобрение.</div>
            </div>

            <button type="submit" class="btn btn-success">Одобри и задай роля</button>
            <a class="btn btn-outline-secondary" asp-action="Details" asp-route-id="@Model.RequestId">Назад</a>
        </form>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
