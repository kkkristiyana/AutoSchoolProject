@model List<AutoSchoolProject.ViewModels.Student.MyLessonListItemViewModel>

@{
    ViewData["Title"] = "Моите часове";
    int studentId = ViewBag.StudentId is int v ? v : 0;
}

<partial name="_StudentPanelNav" />

@if (TempData["Error"] != null)
{
    <div class="alert alert-danger">@TempData["Error"]</div>
}
@if (TempData["Success"] != null)
{
    <div class="alert alert-success">@TempData["Success"]</div>
}

@if (!Model.Any())
{
    <div class="alert alert-info">Курсистът няма записани часове.</div>
}
else
{
    <table class="table table-hover align-middle">
        <thead>
            <tr>
                <th>Дата</th>
                <th>Инструктор</th>
                <th>Категория</th>
                <th>Статус</th>
                <th>Бележка</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var l in Model)
            {
                <tr>
                    <td>@l.DateTime.ToString("dd.MM.yyyy HH:mm")</td>
                    <td>@l.InstructorName</td>
                    <td>@l.CourseName</td>
                    <td>@l.Status</td>
                    <td>@l.Note</td>
                    <td>
                        @if (!l.Completed && l.DateTime > DateTime.Now && (l.Status == "Изчаква одобрение" || l.Status == "Одобрен"))
                        {
                            <form asp-area="Admin" asp-controller="StudentPanel" asp-action="CancelLesson" asp-route-studentId="@studentId" method="post" class="d-inline">
                                @Html.AntiForgeryToken()
                                <input type="hidden" name="id" value="@l.Id" />
                                <button type="submit" class="btn btn-sm btn-outline-danger">Отмени</button>
                            </form>
                        }
                    </td>
                </tr>
            }
        </tbody>
    </table>
}
